#include <stdio.h>
#include "main.h"

/**
 *mul - a function that multiplies two integers
 *
 *Return: always 0 (success)
 */

int mul(int a, int b)
{
int result = a * b;
printf( "%i", result);
return (0);
}
