#include "main.h"

/**
 * main - Entry point
 *
 * Description:prints the alphabet, in lowercase, followed by a new line.
 *
 * Return: 0 (success)
*/

int main(void)
{
for (char i = 'a' ; i <= 'z' ; i++)
putchar(i + 0);
putchar('\n');
return (0);
}
