#include"main.h"

/**
 * _abs - function that computes the absolute value of an intege
 *
 * @int: checks input of function
 *
 * Return: returns always(0) success
*/
int _abs(int)
{
if (n < 0)
n = n * -1;
return (n);
}
